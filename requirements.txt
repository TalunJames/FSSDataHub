fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.9
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.3
pypdf>=5.1
trafilatura>=2.0
pdfplumber>=0.11
pyarrow>=17.0
# Crawlee needs Python 3.10+. On 3.9 the collector falls back to its own
# sequential fetch loop, so a Mac on the system python still runs.
crawlee[httpx,playwright]>=1.9,<2; python_version >= "3.10"
